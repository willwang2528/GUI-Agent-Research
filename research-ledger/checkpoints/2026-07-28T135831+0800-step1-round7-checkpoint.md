# Step 1 Round 7 可恢复检查点

> checkpoint id: `2026-07-28T135831+0800`  
> goal status: **ACTIVE**  
> scientific step: **Step 1 — 证明问题现象与负担**  
> step status: **IN PROGRESS / NO STEP 1 GO**

## 1. 恢复入口

下次继续时按以下顺序读取：

1. `research-ledger/CURRENT_GOAL_STATE.json`
2. 本文件
3. `refine-logs/round-7-stagea-v2-local-source-review.md`
4. `stage0f_osworld2_natural_burden_preregistration.md`
5. `stage0f_osworld2_archived_source_adapter_report.md`
6. `stage0f_production_authority_gap_report.md`
7. `refine-logs/REFINE_STATE.json`

不要从 broad literature survey 重新开始，也不要把 synthetic 或 local-byte 成功写成自然问题证据。

## 2. 当前科研命题

> 长程 GUI 控制的 memory 必须成为可被环境证伪的行动契约；只有当 Agent 知道哪些事实必须仍然为真、为何为真以及错误后如何恢复，Memory 才真正改变能力边界。

仍按七步串行：

1. 证明问题现象与负担；
2. 识别根因；
3. 证明强基线仍有 residual；
4. 证明方法必要性、充分性与组件贡献；
5. 证明 environment-falsification 的独特优势；
6. 审计预算、权限与公平性；
7. retain / rename / stop，并裁决是否改变能力边界。

Steps 2–7 继续锁定，原因是 Step 1 尚未得到自然证据。

## 3. 当前总状态

```text
FRAME-READY
DETAIL-AVAILABILITY-PARTIAL
MEASUREMENT IMPLEMENTATION NOT READY
STAGE A V2 SPECIFIED SYNTHETIC MECHANICS GREEN
PROTOCOL REVISE
LOCAL ARCHIVED BYTES ONLY
NO SYNTHETIC FREEZE
NO BLOCK A
NO STEP 1 GO
```

## 4. 已建立的可执行证据

### 4.1 Stage A v2

本轮 root 复跑：

```text
94/94 Stage A tests PASS
104/104 Stage A + protocol + detail audit + ARIS snapshot PASS
28/28 Ajv 2020 strict schemas PASS
py_compile PASS
```

hash：

```text
schema bundle  e54d495793dce49168e97188cb54b7c50f029c16673d60b128ff7aaa0084e9d6
validator      c014a891f86c0bfba352fcbb06e4f0fcdf3b28e401120617e12d5c86196d679d
A0 input       b6ae5b41036b241d29520bc985b77a37c8c86db7d5097f637eb875e3295804df
tests          6d6496ca7b70471813bb42931f84f9fae1a49c6530d42c99dcd0b872dcafe1a1
negative cases c023df23bafc19cc7d22e1d19d59b3c0f33103ebd66d17e3da03dd76d1cc14de
```

独立 battle 覆盖 X58–X64 与 R02/R04/R06/R07/R09/R10/R11/R12/R15。R11 最终要求 singleton unresolved 保留为 `single_support_no_agreement`，paired disagreement unresolved 保留为 `raw_substantive_disagreement`。

### 4.2 本地 OSWorld2 归档字节

本轮 root 复跑：

```text
22/22 adapter tests PASS
48 local HTML pages
47 replay-bearing pages
1 explicit no-step page
9,138 literal steps
```

hash：

```text
adapter     56cd1e9a3149bcd8eb61da825d8e3cb2d9ded4a9d93b9d81b817b5bb72c5e569
schema      fe9908dbafd5fa0af5b326d4fb63d61b6e18b36137a0861aef0575ed9b0f3e0f
tests       9b5965c628347eb5b5b0f7121594337f7ee6029cecd3d2a0ae19ec2f2dfc9d21
projection  8e2380b5eb436f1dc043ca694a41bfe8cd33e1da3561a4fe48d3eb8bde15473b
receipt     cc27fe03f762b8257d31e1089770281098f3cd0eba7a767c0ce8f39da14079d2
```

允许的唯一证据表述：

```text
LOCAL_ARCHIVED_BYTES_LITERAL_PROJECTION_VERIFIED
SOURCE_ORIGIN_AUTHENTICITY_UNVERIFIED
LOCAL_MANIFEST_SELF_SEALED
TRUSTED_CAPTURE_TIME_MISSING
OBSERVATION_ASSET_AUTHORITY_PARTIAL
PRODUCTION_AUTHORITY_INCOMPLETE
NO BLOCK A
```

## 5. 已经证明与尚未证明

已证明：

- frozen catalog frame 为 648 cells，confirmatory denominator 为 492 cells；
- 本地 48 页归档字节可由固定 parser 得到 47 replay、1 no-step、9,138 steps 的 literal projection；
- 在指定 synthetic inputs 下，Stage A v2 能保留 R/C/P/E/M、resolution mode、unresolved 与 denominator；
- 已知 X/R 反例会在精确 stage/code fail closed；
- production authority 缺失时，当前入口不能生成 Block A PASS。

尚未证明：

- natural UACF-D 的 prevalence、severity 与 preventable terminal loss；
- memory failure 是根因，而非 perception/planning/UI nondeterminism；
- case matcher 对自然轨迹完整；
- screenshot/initial observation/task/evaluator/source origin authority；
- human semantic entailment 的双人独立盲化；
- external time/access/role/raw-roster authority；
- 强基线 residual、方法效果或 capability-boundary change；
- Step 1 GO。

## 6. 下一项唯一实现任务：冻结 A0 raw-case matcher

必须先定义并机械验证：

```text
candidate-edge universe
edge admissibility predicate
maximum-cardinality matching objective
deterministic tie-break
unmatched / singleton handling
both-zero handling
pre-adjudication agreement denominator
a / b / c disagreement ledger
```

最低 negative controls：

- 同一 raw label 被重复匹配；
- greedy 与 maximum-cardinality 产生不同结果；
- outcome 后 tie-break；
- unmatched raw label 被静默删除；
- singleton 被伪装成 agreement；
- both-zero unit 被从 denominator 删除；
- matcher 版本或 hash 与 prereg 不一致；
- matcher 输入 roster 不来自 external commitment。

只有 matcher 冻结后，才有资格说“94-test Stage A mechanics 的 case denominator 对自然 A0 输入有定义”。

## 7. 外部工程状态

- Python 3.12.13 与 `.venv-stage0f-py312` 已创建，但精确依赖仍未安装；不得复制 Python 3.9 native site-packages 冒充 3.12 环境。
- 当前有效回归来自 `.venv-stage0f` 的 Python 3.9.6。
- Git 发布克隆位于 `/private/tmp/gui-agent-research-publish.Uj54FM/repo`。
- 本 checkpoint 创建时，GitHub 推送尚待本轮重新同步、提交与远端 ref 核验；完成后应以 `CURRENT_GOAL_STATE.json` 中的 commit 为准。

## 8. 禁止推断

```text
replay available != environment-falsifiable evidence
local hash closure != official source authenticity
schema/test PASS != natural phenomenon proven
synthetic mechanics ACCEPT != protocol freeze
Stage A ready != Block A authorized
Step 1 in progress != Step 1 GO
```

恢复后必须从 matcher 开始，不能打开真实 outcome、不能生成 Block A、不能进入 Step 2。
